# インフラストラクチャ層

## 📌 概要

インフラストラクチャ層は、技術的な実装詳細を担当するレイヤーです。
データベースアクセス、外部 API 連携、ファイルシステムなど、フレームワークや技術に依存する部分を実装します。

### インフラストラクチャ層の役割

- **永続化の実装**: データベースへのデータ保存・取得
- **外部サービス連携**: 外部 API、メッセージング、ファイル I/O
- **技術的詳細の隠蔽**: ドメイン層が技術に依存しないようにする
- **リポジトリの実装**: ドメイン層のインターフェースを実装

---

## 🏗️ インフラストラクチャ層の構成

```
infrastructure/
  ├ persistence/                ← データ永続化（DB、キャッシュ等）
  │   ├ entity/                     ← JPA エンティティ
  │   │   └ {Aggregate}Entity.java
  │   ├ mapper/                     ← ドメインモデル ↔ JPA エンティティ変換
  │   │   └ {Aggregate}PersistenceMapper.java
  │   └ repository/                 ← リポジトリ実装
  │       ├ {Aggregate}JpaRepository.java      ← Spring Data JPA
  │       └ {Aggregate}RepositoryImpl.java     ← ドメインIF実装
  │
  ├ api/                        ← 外部APIクライアント（将来実装予定）
  │   ├ client/                     ← API クライアント
  │   │   ├ {Service}ApiClient.java
  │   │   └ {Service}ApiClientImpl.java
  │   ├ dto/                        ← API リクエスト/レスポンス DTO
  │   │   ├ {Service}Request.java
  │   │   └ {Service}Response.java
  │   └ config/
  │       └ {Service}ApiConfig.java
  │
  ├ messaging/                  ← メッセージング（Kafka、RabbitMQ等）（将来実装予定）
  │   ├ producer/                   ← メッセージ送信
  │   │   └ {Event}Producer.java
  │   ├ consumer/                   ← メッセージ受信
  │   │   └ {Event}Consumer.java
  │   ├ dto/                        ← メッセージ DTO
  │   │   └ {Event}Message.java
  │   └ config/
  │       └ MessagingConfig.java
  │
  ├ file/                       ← ファイル操作（S3、ローカルストレージ等）（将来実装予定）
  │   ├ storage/                    ← ストレージクライアント
  │   │   ├ FileStorage.java            ← インターフェース
  │   │   ├ S3StorageImpl.java          ← S3 実装
  │   │   └ LocalStorageImpl.java       ← ローカル実装
  │   └ config/
  │       └ FileStorageConfig.java
  │
  ├ mail/                       ← メール送信（将来実装予定）
  │   ├ sender/                     ← メール送信
  │   │   ├ MailSender.java             ← インターフェース
  │   │   └ MailSenderImpl.java
  │   ├ template/                   ← メールテンプレート
  │   │   └ {Type}MailTemplate.java
  │   └ config/
  │       └ MailConfig.java
  │
  ├ security/                   ← 認証・認可の実装（将来実装予定）
  │   ├ auth/                       ← 認証
  │   │   ├ JwtTokenProvider.java
  │   │   └ JwtAuthenticationFilter.java
  │   ├ encryption/                 ← 暗号化
  │   │   └ PasswordEncoder.java
  │   └ config/
  │       └ SecurityConfig.java
  │
  └ config/                     ← インフラ関連の設定
      ├ JpaConfig.java              ← JPA 設定
      ├ CacheConfig.java            ← キャッシュ設定
      └ CorsConfig.java             ← CORS 設定
```

### 現在実装済み: persistence（データ永続化）

**例: Film 集約の場合**

```
infrastructure/
  └ persistence/
      ├ entity/
      │   └ FilmEntity.java
      ├ mapper/
      │   └ FilmPersistenceMapper.java
      └ repository/
          ├ FilmJpaRepository.java
          └ FilmRepositoryImpl.java
```

---

### 将来実装予定

#### 1. api（外部APIクライアント）

外部サービスの REST API と連携するためのクライアント実装。

**例: 決済 API の場合**

```
api/
  ├ client/
  │   ├ PaymentApiClient.java           ← インターフェース
  │   └ PaymentApiClientImpl.java       ← RestTemplate/WebClient 実装
  ├ dto/
  │   ├ PaymentRequest.java             ← API リクエスト DTO
  │   └ PaymentResponse.java            ← API レスポンス DTO
  └ config/
      └ PaymentApiConfig.java           ← API 設定（URL、認証情報等）
```

**実装例**:

```java
public interface PaymentApiClient {
    PaymentResponse processPayment(PaymentRequest request);
}

@Component
@RequiredArgsConstructor
public class PaymentApiClientImpl implements PaymentApiClient {
    private final RestTemplate restTemplate;
    private final PaymentApiConfig config;

    @Override
    public PaymentResponse processPayment(PaymentRequest request) {
        return restTemplate.postForObject(
            config.getBaseUrl() + "/payments",
            request,
            PaymentResponse.class
        );
    }
}
```

---

#### 2. messaging（メッセージング）

Kafka、RabbitMQ 等のメッセージングシステムとの連携。

**例: 注文イベントの場合**

```
messaging/
  ├ producer/
  │   └ OrderEventProducer.java         ← イベント送信
  ├ consumer/
  │   └ OrderEventConsumer.java         ← イベント受信
  ├ dto/
  │   └ OrderEventMessage.java          ← イベントメッセージ DTO
  └ config/
      └ KafkaConfig.java                ← Kafka 設定
```

**Producer 実装例**:

```java
@Component
@RequiredArgsConstructor
public class OrderEventProducer {
    private final KafkaTemplate<String, OrderEventMessage> kafkaTemplate;

    public void publishOrderCreated(Order order) {
        OrderEventMessage message = new OrderEventMessage(
            order.getId().getValue(),
            order.getStatus().name(),
            LocalDateTime.now()
        );
        kafkaTemplate.send("order-events", message);
    }
}
```

**Consumer 実装例**:

```java
@Component
@Slf4j
public class OrderEventConsumer {

    @KafkaListener(topics = "order-events", groupId = "order-service")
    public void consumeOrderEvent(OrderEventMessage message) {
        log.info("Received order event: {}", message);
        // イベント処理ロジック
    }
}
```

---

#### 3. file（ファイル操作）

S3、ローカルストレージ等へのファイル保存・取得。

**例: S3 ストレージの場合**

```
file/
  ├ storage/
  │   ├ FileStorage.java                ← インターフェース
  │   ├ S3StorageImpl.java              ← S3 実装
  │   └ LocalStorageImpl.java           ← ローカル実装
  └ config/
      └ S3Config.java                   ← S3 設定
```

**インターフェース**:

```java
public interface FileStorage {
    String upload(String key, byte[] content);
    byte[] download(String key);
    void delete(String key);
}
```

**S3 実装例**:

```java
@Component
@RequiredArgsConstructor
public class S3StorageImpl implements FileStorage {
    private final AmazonS3 s3Client;
    private final S3Config config;

    @Override
    public String upload(String key, byte[] content) {
        ObjectMetadata metadata = new ObjectMetadata();
        metadata.setContentLength(content.length);

        s3Client.putObject(
            config.getBucketName(),
            key,
            new ByteArrayInputStream(content),
            metadata
        );

        return s3Client.getUrl(config.getBucketName(), key).toString();
    }

    @Override
    public byte[] download(String key) {
        S3Object s3Object = s3Client.getObject(config.getBucketName(), key);
        return s3Object.getObjectContent().readAllBytes();
    }
}
```

---

#### 4. mail（メール送信）

JavaMailSender を使用したメール送信機能。

**例: ユーザー登録メールの場合**

```
mail/
  ├ sender/
  │   ├ MailSender.java                 ← インターフェース
  │   └ MailSenderImpl.java             ← JavaMailSender 実装
  ├ template/
  │   ├ WelcomeMailTemplate.java        ← ウェルカムメール
  │   └ PasswordResetMailTemplate.java  ← パスワードリセット
  └ config/
      └ MailConfig.java                 ← SMTP 設定
```

**実装例**:

```java
public interface MailSender {
    void send(String to, String subject, String body);
}

@Component
@RequiredArgsConstructor
public class MailSenderImpl implements MailSender {
    private final JavaMailSender javaMailSender;

    @Override
    public void send(String to, String subject, String body) {
        SimpleMailMessage message = new SimpleMailMessage();
        message.setTo(to);
        message.setSubject(subject);
        message.setText(body);
        javaMailSender.send(message);
    }
}

@Component
public class WelcomeMailTemplate {
    public String generateBody(String userName) {
        return String.format(
            "Welcome %s!\n\nThank you for registering.",
            userName
        );
    }
}
```

---

#### 5. security（認証・認可）

JWT 認証、パスワード暗号化などのセキュリティ機能。

**例: JWT 認証の場合**

```
security/
  ├ auth/
  │   ├ JwtTokenProvider.java           ← JWT トークン生成・検証
  │   └ JwtAuthenticationFilter.java    ← JWT 認証フィルター
  ├ encryption/
  │   └ PasswordEncoderImpl.java        ← パスワード暗号化
  └ config/
      └ SecurityConfig.java             ← Spring Security 設定
```

**JWT Provider 実装例**:

```java
@Component
public class JwtTokenProvider {

    @Value("${jwt.secret}")
    private String secretKey;

    @Value("${jwt.expiration}")
    private long validityInMilliseconds;

    public String createToken(String username, List<String> roles) {
        Claims claims = Jwts.claims().setSubject(username);
        claims.put("roles", roles);

        Date now = new Date();
        Date validity = new Date(now.getTime() + validityInMilliseconds);

        return Jwts.builder()
            .setClaims(claims)
            .setIssuedAt(now)
            .setExpiration(validity)
            .signWith(SignatureAlgorithm.HS256, secretKey)
            .compact();
    }

    public boolean validateToken(String token) {
        try {
            Jwts.parser().setSigningKey(secretKey).parseClaimsJws(token);
            return true;
        } catch (JwtException | IllegalArgumentException e) {
            return false;
        }
    }
}
```

**Security Config 実装例**:

```java
@Configuration
@EnableWebSecurity
public class SecurityConfig {

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .csrf().disable()
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/api/auth/**").permitAll()
                .requestMatchers("/api/admin/**").hasRole("ADMIN")
                .anyRequest().authenticated()
            )
            .sessionManagement()
                .sessionCreationPolicy(SessionCreationPolicy.STATELESS);

        return http.build();
    }

    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}
```

---

#### 6. config（インフラ設定）

各種インフラストラクチャの設定クラスを集約。

```
config/
  ├ JpaConfig.java                  ← JPA/Hibernate 設定
  ├ CacheConfig.java                ← Redis キャッシュ設定
  ├ CorsConfig.java                 ← CORS 設定
  └ AsyncConfig.java                ← 非同期処理設定
```

**JPA Config 実装例**:

```java
@Configuration
@EnableJpaAuditing
public class JpaConfig {

    @Bean
    public AuditorAware<String> auditorProvider() {
        return () -> Optional.of("system");
    }
}
```

**CORS Config 実装例**:

```java
@Configuration
public class CorsConfig {

    @Bean
    public WebMvcConfigurer corsConfigurer() {
        return new WebMvcConfigurer() {
            @Override
            public void addCorsMappings(CorsRegistry registry) {
                registry.addMapping("/api/**")
                    .allowedOrigins("http://localhost:3000")
                    .allowedMethods("GET", "POST", "PUT", "DELETE", "PATCH")
                    .allowedHeaders("*")
                    .allowCredentials(true);
            }
        };
    }
}
```

---

## 🎯 インフラストラクチャ層の構成要素

### 1. JPA エンティティ（Entity）

JPA エンティティは、データベースのテーブルとマッピングされる Java クラスです。

#### 基本パターン

```java
@Entity
@Table(name = "{table_name}")
@Data
@NoArgsConstructor
@AllArgsConstructor
public class {Aggregate}Entity {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    @Column(name = "id")
    private Long id;

    @Column(name = "property1", nullable = false)
    private String property1;

    @Column(name = "property2", length = 500)
    private String property2;

    @Column(name = "property3")
    private Integer property3;

    @Column(name = "created_at", nullable = false, updatable = false)
    private LocalDateTime createdAt;

    @Column(name = "updated_at")
    private LocalDateTime updatedAt;
}
```

#### 設計のポイント

1. **@Entity アノテーション**
   - JPA エンティティであることを示す
   - Hibernate が管理するオブジェクト

2. **@Table アノテーション**
   - マッピングするテーブル名を指定
   - 省略時はクラス名がテーブル名になる

3. **@Id と @GeneratedValue**
   - 主キーを指定
   - `GenerationType.IDENTITY`: DB の AUTO_INCREMENT を使用
   - `GenerationType.SEQUENCE`: シーケンスを使用（PostgreSQL など）
   - `GenerationType.AUTO`: JPA プロバイダが自動選択

4. **@Column アノテーション**
   - カラム名、制約を指定
   - `nullable`: NULL 許可/不許可
   - `length`: 文字列の長さ
   - `unique`: ユニーク制約
   - `updatable`: 更新可能/不可

5. **プリミティブ型を使用**
   - ドメインモデルとは異なり、プリミティブ型を使用
   - JPA とのマッピングが容易

---

#### JPA アノテーションの詳細

##### 基本的なマッピング

```java
// カラム名の明示的な指定
@Column(name = "user_name")
private String userName;

// NULL 不可
@Column(nullable = false)
private String requiredField;

// ユニーク制約
@Column(unique = true)
private String email;

// 文字列の長さ指定
@Column(length = 100)
private String shortText;

// TEXT 型
@Column(columnDefinition = "TEXT")
private String longText;

// 更新不可（作成時のみ設定）
@Column(updatable = false)
private LocalDateTime createdAt;

// 挿入不可（更新時のみ設定）
@Column(insertable = false)
private LocalDateTime lastModifiedAt;
```

##### 日時フィールド

```java
// タイムスタンプ（自動設定）
@CreatedDate
@Column(name = "created_at", updatable = false)
private LocalDateTime createdAt;

@LastModifiedDate
@Column(name = "updated_at")
private LocalDateTime updatedAt;

// エンティティリスナーを有効化
@EntityListeners(AuditingEntityListener.class)
public class FilmEntity {
    // ...
}
```

**Spring Data JPA の監査機能を有効化**:

```java
@Configuration
@EnableJpaAuditing
public class JpaConfig {
}
```

##### リレーションシップ

###### 多対一（@ManyToOne）

```java
@Entity
public class OrderEntity {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    // 多対一: 多数の注文が1人の顧客に属する
    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "customer_id", nullable = false)
    private CustomerEntity customer;
}
```

###### 一対多（@OneToMany）

```java
@Entity
public class CustomerEntity {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    // 一対多: 1人の顧客が多数の注文を持つ
    @OneToMany(mappedBy = "customer", cascade = CascadeType.ALL, orphanRemoval = true)
    private List<OrderEntity> orders = new ArrayList<>();
}
```

###### 一対一（@OneToOne）

```java
@Entity
public class UserEntity {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    // 一対一: 1人のユーザーが1つのプロフィールを持つ
    @OneToOne(cascade = CascadeType.ALL)
    @JoinColumn(name = "profile_id", unique = true)
    private ProfileEntity profile;
}
```

###### 多対多（@ManyToMany）

```java
@Entity
public class StudentEntity {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    // 多対多: 多数の学生が多数のコースを受講
    @ManyToMany
    @JoinTable(
        name = "student_course",
        joinColumns = @JoinColumn(name = "student_id"),
        inverseJoinColumns = @JoinColumn(name = "course_id")
    )
    private Set<CourseEntity> courses = new HashSet<>();
}
```

##### FetchType の選択

```java
// LAZY（推奨）: 必要になったときに取得
@ManyToOne(fetch = FetchType.LAZY)
private CustomerEntity customer;

// EAGER: エンティティと一緒に即座に取得
@ManyToOne(fetch = FetchType.EAGER)
private CustomerEntity customer;
```

**推奨**: 基本的に `FetchType.LAZY` を使用し、N+1 問題は `@EntityGraph` や JOIN FETCH で対応

---

### 2. 永続化マッパー（Persistence Mapper）

永続化マッパーは、ドメインモデルと JPA エンティティの相互変換を行います。

#### 基本パターン

```java
@Component
public class {Aggregate}PersistenceMapper {

    /**
     * ドメインモデル → JPA エンティティ
     */
    public {Aggregate}Entity toEntity({Aggregate} domain) {
        if (domain == null) {
            return null;
        }

        {Aggregate}Entity entity = new {Aggregate}Entity();

        // ID の変換
        if (domain.getId() != null) {
            entity.setId(domain.getId().getValue());
        }

        // 値オブジェクト → プリミティブ型
        entity.setProperty1(domain.getProperty1().getValue());
        entity.setProperty2(domain.getProperty2().getValue());
        entity.setProperty3(domain.getProperty3().getValue());

        return entity;
    }

    /**
     * JPA エンティティ → ドメインモデル
     */
    public {Aggregate} toDomain({Aggregate}Entity entity) {
        if (entity == null) {
            return null;
        }

        // ドメインモデルの再構成
        return {Aggregate}.reconstruct(
            new {Aggregate}Id(entity.getId()),
            new Property1(entity.getProperty1()),
            new Property2(entity.getProperty2()),
            new Property3(entity.getProperty3())
        );
    }

    /**
     * エンティティリスト → ドメインモデルリスト
     */
    public List<{Aggregate}> toDomainList(List<{Aggregate}Entity> entities) {
        return entities.stream()
            .map(this::toDomain)
            .collect(Collectors.toList());
    }
}
```

#### 設計のポイント

1. **値オブジェクトの変換**
   - ドメインモデル: 値オブジェクト（`Property`）
   - JPA エンティティ: プリミティブ型（`String`, `Integer`）

2. **ファクトリーメソッドの使用**
   - `reconstruct()` で既存データからドメインモデルを復元
   - `create()` は使用しない（新規作成ではない）

3. **null チェック**
   - null の場合は null を返す
   - NullPointerException を防ぐ

4. **Stream API の活用**
   - リスト変換に Stream API を使用
   - コードが簡潔になる

---

### 3. Spring Data JPA リポジトリ

Spring Data JPA のインターフェースを継承し、基本的な CRUD 操作を自動実装します。

#### 基本パターン

```java
@Repository
public interface {Aggregate}JpaRepository extends JpaRepository<{Aggregate}Entity, Long> {

    /**
     * カスタムクエリメソッド
     */

    // プロパティで検索
    Optional<{Aggregate}Entity> findByProperty1(String property1);

    // 複数条件で検索
    List<{Aggregate}Entity> findByProperty1AndProperty2(String property1, String property2);

    // LIKE 検索
    List<{Aggregate}Entity> findByProperty1Containing(String keyword);

    // 範囲検索
    List<{Aggregate}Entity> findByProperty3Between(Integer min, Integer max);

    // 存在確認
    boolean existsByProperty1(String property1);

    // 削除
    void deleteByProperty1(String property1);

    // @Query アノテーションを使用したカスタムクエリ
    @Query("SELECT a FROM {Aggregate}Entity a WHERE a.property1 = :property1 AND a.property3 > :minValue")
    List<{Aggregate}Entity> findByCustomCondition(
        @Param("property1") String property1,
        @Param("minValue") Integer minValue
    );

    // ネイティブクエリ
    @Query(value = "SELECT * FROM {table_name} WHERE property1 = :property1", nativeQuery = true)
    List<{Aggregate}Entity> findByNativeQuery(@Param("property1") String property1);
}
```

#### Spring Data JPA のメソッド命名規則

```java
// 等価検索
findBy{Property}(Type value)

// AND 条件
findBy{Property1}And{Property2}(Type1 value1, Type2 value2)

// OR 条件
findBy{Property1}Or{Property2}(Type1 value1, Type2 value2)

// LIKE 検索
findBy{Property}Containing(String value)  // %value%
findBy{Property}StartingWith(String value)  // value%
findBy{Property}EndingWith(String value)  // %value

// 比較演算
findBy{Property}LessThan(Type value)  // <
findBy{Property}LessThanEqual(Type value)  // <=
findBy{Property}GreaterThan(Type value)  // >
findBy{Property}GreaterThanEqual(Type value)  // >=

// 範囲検索
findBy{Property}Between(Type min, Type max)

// IN 句
findBy{Property}In(Collection<Type> values)

// NULL チェック
findBy{Property}IsNull()
findBy{Property}IsNotNull()

// ソート
findBy{Property}OrderBy{Property2}Asc(Type value)
findBy{Property}OrderBy{Property2}Desc(Type value)

// 上位 N 件
findTop10By{Property}(Type value)
findFirst5By{Property}(Type value)

// 存在確認
existsBy{Property}(Type value)

// カウント
countBy{Property}(Type value)

// 削除
deleteBy{Property}(Type value)
```

---

### 4. リポジトリ実装（RepositoryImpl）

ドメイン層のリポジトリインターフェースを実装するクラスです。

#### 基本パターン

```java
@Repository
@RequiredArgsConstructor
public class {Aggregate}RepositoryImpl implements {Aggregate}Repository {

    private final {Aggregate}JpaRepository jpaRepository;
    private final {Aggregate}PersistenceMapper mapper;

    @Override
    public {Aggregate} save({Aggregate} aggregate) {
        // 1. ドメインモデル → JPA エンティティ
        {Aggregate}Entity entity = mapper.toEntity(aggregate);

        // 2. データベースに保存
        {Aggregate}Entity savedEntity = jpaRepository.save(entity);

        // 3. JPA エンティティ → ドメインモデル
        return mapper.toDomain(savedEntity);
    }

    @Override
    public void delete({Aggregate} aggregate) {
        {Aggregate}Entity entity = mapper.toEntity(aggregate);
        jpaRepository.delete(entity);
    }

    @Override
    public Optional<{Aggregate}> findById({Aggregate}Id id) {
        return jpaRepository.findById(id.getValue())
            .map(mapper::toDomain);
    }

    @Override
    public Optional<{Aggregate}> findBy{Property}({Property} property) {
        return jpaRepository.findBy{Property}(property.getValue())
            .map(mapper::toDomain);
    }

    @Override
    public List<{Aggregate}> findAll() {
        return jpaRepository.findAll()
            .stream()
            .map(mapper::toDomain)
            .collect(Collectors.toList());
    }

    @Override
    public {Aggregate}Id nextIdentity() {
        // ID 生成ロジック
        // 例: UUID、タイムスタンプ、シーケンス など
        return new {Aggregate}Id(UUID.randomUUID().toString());
    }
}
```

#### 設計のポイント

1. **@Repository アノテーション**
   - Spring の永続化層コンポーネントとして登録
   - 例外の変換（SQLException → DataAccessException）

2. **依存性注入**
   - `{Aggregate}JpaRepository`: Spring Data JPA
   - `{Aggregate}PersistenceMapper`: ドメイン ↔ エンティティ変換

3. **値オブジェクトの扱い**
   - ドメインリポジトリは値オブジェクトを受け取る
   - `.getValue()` でプリミティブ型に変換して JPA リポジトリに渡す

4. **Optional の活用**
   - 検索結果が存在しない可能性を明示
   - `map()` でエンティティをドメインモデルに変換

5. **Stream API**
   - リスト変換を簡潔に記述

---

## 🔍 インフラストラクチャ層の設計原則

### 1. ドメイン層への非依存

❌ **悪い例**: ドメインモデルを直接返す

```java
@Repository
public interface FilmJpaRepository extends JpaRepository<Film, Long> {
    // NG: JPA リポジトリがドメインモデルを扱う
}
```

✅ **良い例**: JPA エンティティを扱い、実装クラスで変換

```java
// JPA リポジトリは JPA エンティティを扱う
@Repository
public interface FilmJpaRepository extends JpaRepository<FilmEntity, Long> {
}

// 実装クラスでドメインモデルに変換
@Repository
public class FilmRepositoryImpl implements FilmRepository {
    public Film save(Film film) {
        FilmEntity entity = mapper.toEntity(film);
        FilmEntity saved = jpaRepository.save(entity);
        return mapper.toDomain(saved);
    }
}
```

---

### 2. 技術的詳細の隠蔽

✅ **良い例**: ドメイン層に技術を露出しない

```java
// ドメイン層: 技術非依存のインターフェース
public interface FilmRepository {
    Film save(Film film);
    Optional<Film> findById(FilmId id);
}

// インフラ層: JPA を使った実装
@Repository
public class FilmRepositoryImpl implements FilmRepository {
    private final FilmJpaRepository jpaRepository;  // JPA
    // ...
}
```

---

### 3. マッパーによる分離

✅ **良い例**: マッパーで変換を集約

```java
@Component
public class FilmPersistenceMapper {
    public FilmEntity toEntity(Film film) {
        // 変換ロジック
    }

    public Film toDomain(FilmEntity entity) {
        // 変換ロジック
    }
}
```

❌ **悪い例**: リポジトリ内で直接変換

```java
@Repository
public class FilmRepositoryImpl implements FilmRepository {
    public Film save(Film film) {
        FilmEntity entity = new FilmEntity();
        entity.setId(film.getId().getValue());  // 変換が散らばる
        // ...
    }
}
```

---

## 🚀 実践的なパターン

### 1. ページネーション対応

```java
@Repository
@RequiredArgsConstructor
public class {Aggregate}RepositoryImpl implements {Aggregate}Repository {

    private final {Aggregate}JpaRepository jpaRepository;
    private final {Aggregate}PersistenceMapper mapper;

    @Override
    public Page<{Aggregate}> findAll(Pageable pageable) {
        Page<{Aggregate}Entity> entityPage = jpaRepository.findAll(pageable);
        return entityPage.map(mapper::toDomain);
    }

    @Override
    public Page<{Aggregate}> findBy{Condition}(
        {Condition} condition,
        Pageable pageable
    ) {
        Page<{Aggregate}Entity> entityPage =
            jpaRepository.findBy{Property}(
                condition.getValue(),
                pageable
            );
        return entityPage.map(mapper::toDomain);
    }
}
```

---

### 2. カスタムクエリの実装

```java
@Repository
public interface {Aggregate}JpaRepository extends JpaRepository<{Aggregate}Entity, Long> {

    /**
     * JPQL を使用したカスタムクエリ
     */
    @Query("SELECT a FROM {Aggregate}Entity a " +
           "WHERE a.property1 = :property1 " +
           "AND a.property3 > :minValue " +
           "ORDER BY a.createdAt DESC")
    List<{Aggregate}Entity> findByCustomCondition(
        @Param("property1") String property1,
        @Param("minValue") Integer minValue
    );

    /**
     * ネイティブクエリ
     */
    @Query(value = "SELECT * FROM {table_name} " +
                   "WHERE property1 = :property1 " +
                   "AND property3 BETWEEN :min AND :max",
           nativeQuery = true)
    List<{Aggregate}Entity> findByNativeQuery(
        @Param("property1") String property1,
        @Param("min") Integer min,
        @Param("max") Integer max
    );

    /**
     * 動的クエリ（Specification パターン）
     */
    @Query("SELECT a FROM {Aggregate}Entity a WHERE " +
           "(:property1 IS NULL OR a.property1 = :property1) AND " +
           "(:minValue IS NULL OR a.property3 >= :minValue)")
    List<{Aggregate}Entity> findByDynamicCondition(
        @Param("property1") String property1,
        @Param("minValue") Integer minValue
    );
}
```

---

### 3. トランザクション管理

トランザクションはアプリケーション層で管理しますが、インフラ層でも制御可能です。

```java
@Repository
@RequiredArgsConstructor
public class {Aggregate}RepositoryImpl implements {Aggregate}Repository {

    @Transactional(readOnly = true)  // 読み取り専用トランザクション
    @Override
    public List<{Aggregate}> findAll() {
        return jpaRepository.findAll()
            .stream()
            .map(mapper::toDomain)
            .collect(Collectors.toList());
    }

    @Transactional  // 書き込みトランザクション
    @Override
    public {Aggregate} save({Aggregate} aggregate) {
        FilmEntity entity = mapper.toEntity(aggregate);
        FilmEntity saved = jpaRepository.save(entity);
        return mapper.toDomain(saved);
    }
}
```

**推奨**: トランザクションはユースケース（アプリケーション層）で管理

---

### 4. N+1 問題の対策

#### Entity Graph の使用

```java
@Repository
public interface {Aggregate}JpaRepository extends JpaRepository<{Aggregate}Entity, Long> {

    @EntityGraph(attributePaths = {"relatedEntity", "anotherRelation"})
    @Query("SELECT a FROM {Aggregate}Entity a WHERE a.property1 = :property1")
    List<{Aggregate}Entity> findWithRelations(@Param("property1") String property1);
}
```

#### JOIN FETCH の使用

```java
@Query("SELECT a FROM {Aggregate}Entity a " +
       "LEFT JOIN FETCH a.relatedEntity " +
       "WHERE a.property1 = :property1")
List<{Aggregate}Entity> findWithRelations(@Param("property1") String property1);
```

---

## 📝 まとめ

### インフラストラクチャ層の重要ポイント

✅ **技術的実装の集約**

- データベースアクセス
- 外部サービス連携

✅ **ドメイン層の保護**

- 技術的詳細を隠蔽
- インターフェースを実装

✅ **マッパーによる変換**

- ドメインモデル ↔ JPA エンティティ
- 値オブジェクト ↔ プリミティブ型

✅ **Spring Data JPA の活用**

- 基本的な CRUD を自動実装
- カスタムクエリメソッド

✅ **依存関係の方向**

- ドメイン層のインターフェースに依存
- ドメイン層は インフラ層に非依存

---

## 🔗 関連ドキュメント

- [アーキテクチャ設計.md](../アーキテクチャ設計.md) - 全体アーキテクチャの説明
- [ドメイン層.md](./ドメイン層.md) - ドメインモデルの詳細
- [アプリケーション層.md](./アプリケーション層.md) - ユースケースの実装
- [プレゼンテーション層.md](./プレゼンテーション層.md) - コントローラーの実装
